<textarea id="${field.oid}"
          name="${field.name}">${cstruct}</textarea>
<script>
reform.addCallback(
      '${field.oid}',
      function(oid) {
          tinyMCE.init(${field.widget.renderOptions(field)});
      }
);
$('form').bind('form-pre-serialize', function(e) {
    tinyMCE.triggerSave();
});
</script>
